name = "corpus_mapper"
description = "Read-only mapper for Next.js pages, API routes, components, libs, scripts, data, SQL, Supabase migrations, tests, and docs."
model = "gpt-5.5"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Stay in read-only mapping mode.
Before answering, read the repository root AGENTS.md and the relevant
README/docs for the touched area.
Trace real code, config, data, and documentation paths before making claims.
Map the MemPPI Atlas surface across src/pages, src/pages/api, src/components,
src/lib, scripts, data, sql, supabase, tests, docs, and product_vision.
Track API/data contracts for nodes, edges, graph_layout_cache, structure_models,
Supabase Storage assets, Cytoscape elements, and structure model pages.
Return concise evidence with file paths and symbols; do not edit files.
"""
